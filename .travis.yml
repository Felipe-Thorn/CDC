language: C

compiler:
  - gcc
 
before_script:
  - mkdir build
  - mkdir headers
  - cd lib
  - ls
  - cd ../build
  - cmake ..
  
script: 
  - make
  - sudo make install
  
after_script:
 - ctest -V
 - cd ../lib
 - ls
 - cd ..
 - ls
 - runSistema.sh
